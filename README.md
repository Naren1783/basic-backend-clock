# Basic Backend Clock ⏰

A simple digital clock built to demonstrate how a frontend web page communicates with a custom backend API. 

The backend serves the current time via JSON, and the frontend polls the API every second to update the display. No external frameworks required!

## How to run it

1. Clone this repository to your local machine.
2. Open your terminal and start the server:
   ```bash
   python server.py
